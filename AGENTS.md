# Agent Notes

本项目是个人服务器 Web 控制台，后续 Codex session 在开发相关功能前，应先阅读以下文档以理解产品边界和技术方向：

- `docs/personal-web-terminal-product-features.md`
- `docs/personal-web-terminal-technical-design.md`
- `docs/happy-technical-reference.md`

其中 `docs/happy-technical-reference.md` 是对 `slopus/happy` 的技术参考建议。它只作为设计参考，不是强制实现规范；开发时仍应优先遵循本项目的 Go 后端、SQLite、SSE、个人单机部署和受控权限边界。

## 公开依赖与信息边界

本仓库面向 GitHub 公开托管，所有提交内容都必须避免泄露个人或组织私有信息。后续新增、修改或生成文件时必须遵守：

- Go、npm、GitHub Actions、脚本、文档和示例配置中的依赖下载源必须使用公共可访问资源，例如公开 Go module proxy、公开 npm registry 和公开 GitHub release/source URL。
- 不允许提交非公开网络地址、组织私有镜像、私有包仓库、私有 API 地址、代理地址、内部系统名称、内部工单或审批链接。
- 不允许提交个人本机绝对路径、个人用户名、个人邮箱、真实服务器地址、私有仓库 URL、访问 token、cookie、API Key、密钥、证书、数据库文件或包含真实凭据的配置。
- lockfile、`.npmrc`、Go 环境变量、CI 配置和安装脚本在提交前必须检查依赖来源；如果发现非公共来源，必须改成公共来源或移除。
- 文档中的示例路径、账号、域名、bucket、endpoint 和 token 必须使用通用占位值，不得使用真实个人或私有值。

进行前端设计、UI 优化、视觉重构、交互体验调整或前端设计审查时，必须先使用 `design-taste-frontend`（Design Taste Frontend）skill 进行设计判断和 pre-flight 检查。由于本项目是个人服务器 Web 控制台，不是营销页或作品集页面，使用该 skill 时应优先采用其中关于审美校准、可访问性、响应式、色彩一致性、形状一致性、交互状态和反 AI-slop 的规则；不应机械套用 landing page、hero、logo wall、营销 CTA 等不适合控制台产品的规则。

## 前端美术风格

本项目前端应采用 **Quiet Agent Workbench / Quiet DevOps Control Plane** 风格：面向技术用户的安静、克制、工程化工作台，而不是营销页、传统后台模板或装饰型 dashboard。整体应接近 Linear、Raycast、Cursor、Cloudflare dashboard 这类 devtool / control plane 审美，但必须服务个人服务器 Web 控制台的任务流。

核心风格约束：

- 页面气质应冷静、低装饰、强功能导向。避免品牌化大视觉、插画、渐变背景、玻璃拟态、AI 紫蓝光、营销 hero、logo wall、夸张 CTA 和为了“高级感”加入的装饰动效。
- 默认使用浅色中性底：白色、浅灰、近黑文字、细边框、低对比 hover/selected 背景和非常轻的阴影。主色只保留一个克制强调色；状态色必须有语义，橙色用于 warning/stale/offline，绿色用于 available/on/success，红色只用于危险操作。
- 布局优先采用工作台结构：左侧全局导航、上下文列表、主工作区、右侧详情 inspector 可以常驻，但必须低噪音、窄栏、细分隔、少文本。不要把常驻区域做成厚重卡片或高饱和导航。
- 页面主内容应围绕当前任务的核心对象展开，例如会话、设备、任务、进程、终端或配置对象。首屏优先呈现当前状态、主操作、风险提示和必要上下文；过滤器、批量操作、资源配置、历史记录、帮助说明、调试信息等应优先收进弹窗、抽屉、popover、折叠详情、右侧 inspector 或二级页面。
- 空状态应产品化但克制：可以使用居中的轻量 onboarding 操作卡片、简短说明和下一步入口；不要使用大插画、大标题、营销文案或占满页面的欢迎区。
- 信息密度应随场景变化：聊天、终端、任务执行页保持稀疏和沉浸；设备详情、活动指标、能力矩阵、运行诊断页可以提高密度，但仍使用小字号、细分隔、轻卡片和稳定网格，而不是彩色仪表盘。
- 组件形状必须统一：小圆角、细边框、轻背景为主。按钮、输入框、列表项、卡片、popover、drawer 的圆角和边框强度应保持同一体系；不要混用大圆角消费级卡片、胶囊按钮和尖角表格。
- 字体层级应偏工程产品：正文和 UI 使用清晰 sans-serif，小字号但可读；命令、路径、版本号、设备名、provider、环境变量、日志片段等技术值应使用 monospace 或等宽风格。不要使用 serif、展示字体或大幅 letter spacing。
- 图标应使用统一线性图标族，尺寸小、描边轻，只辅助识别。不要手绘临时 SVG，不要混用多套图标，不要用 emoji 承担常规 UI 状态。
- 交互状态要完整但低调：hover、selected、focus、disabled、loading、empty、error、stale、offline、danger 都要可辨认；焦点态必须可访问，但不应破坏整体克制风格。
- 页面应尽量减少视觉噪音，而不是机械减少所有常驻元素。安全状态、运行状态、中断/回退入口、当前对象上下文和右侧诊断 inspector 如果是当前任务的核心，就应该持续可见；非核心内容才应被折叠或延后展示。

## 信息架构与导航约束

- 一级导航只放真正的全局产品区域，例如控制台、Codex Gateway、日志、Images、V2Ray、设置等。不要把同一能力域里的对象管理、执行历史、权限边界、状态面板等并列拆成多个一级 tab。
- 当页面对象明显归属于某个能力域时，必须优先使用多级 tab、上下文列、抽屉或 inspector 收纳。例如某个能力域内的子视图、运行历史和诊断信息应作为该能力域下的二级导航，而不是与其并列提升为一级 tab。
- 每个独立功能域的设置项都必须放在该功能域自己的一级导航或其下属二级页面中。例如 V2Ray 的监听端口、传输协议、TLS、远程设备凭据和启动策略只能放在 `V2Ray` 页面下，不能塞进通用 `设置` 页面。
- `设置` 这类大的通用导航只能承载控制台全局设置，例如允许根目录、Cookie 策略、全局运行参数、默认路径、全局认证/安全策略和跨功能的系统级配置。不要把某个独立功能的配置、状态面板、操作按钮或 onboarding 入口放到通用设置页里。
- 控制台首页可以展示各功能域的摘要状态和跳转入口，但不能在首页直接展开独立功能的完整配置表单；完整配置必须回到对应一级导航下。
- 多级 tab 应表达对象层级和任务流：一级选择能力域，二级选择该域内的视图或对象类型，三级才进入具体列表、详情或操作面板。不要为了减少点击把互相依赖的上下文平铺成同一层。
- 二级导航应保持 Quiet Agent Workbench 风格：小字号、细边框、低对比选中态、可横向滚动的响应式布局。不要使用营销式大 tab、彩色胶囊、装饰性图标或与主导航冲突的高饱和样式。
- 引入新模块时先判断它是全局能力、某能力域的子视图，还是当前对象的 inspector 信息。只有跨多个能力域独立存在、且有完整任务流的模块，才提升为一级导航。

前端实现时应避免以下反模式：

- 不要把页面入口文件写成几百行的大杂烩，再由 agent 根据代码量、函数数量或状态复杂度自行判断是否拆分。页面入口应主要负责路由、数据装配、状态协调和模块组合。
- 不要在页面主入口里连续堆叠多个临时 `function Xxx()` 或匿名局部组件，导致文件变成组件仓库。稳定的区域、表格、表单、弹窗和工具条应按业务语义拆成命名组件。
- 不要自研每个详情页的外壳，例如返回按钮、标题区、loading 容器、操作区和内容区。优先复用项目已有的详情页容器、布局组件或同域页面的既定结构。
- 不要把强关联的区域拆成同目录下大量零散文件，让样式、状态和业务流程分散在多个无边界小文件中。更适合使用 `components/<FeatureName>/index.tsx` 这类聚合结构时，应让内部组件围绕同一业务语义组织。
- 不要把表格列定义、筛选转换、状态文案、资源规格格式化、权限判断等可测试逻辑直接塞进 JSX render 内部。应抽到同文件的纯函数、领域 helper 或相邻模块中。
- 不要为了“少文件”复用无语义关联的 style class，也不要让父页面用深层选择器控制子组件内部结构。样式边界应跟随组件边界，跨组件样式只能表达布局关系。
- 不要添加空泛注释，例如“渲染基本信息”“点击按钮”。注释只解释业务约束、兼容原因、风险边界或未来切换点。

## 日志打印约束

本项目的日志必须服务在线排障和安全审计，数量可控、结构清晰、默认脱敏。后续新增或修改功能时，应区分三类记录：

- 服务运行日志：用于排查 Go 服务、子进程、外部 provider、日志读取和后台任务问题，优先使用 structured `slog`。
- 业务审计：用于记录 owner 操作、安全决策、权限变化和风险摘要，写入 audit，不用普通服务日志替代。
- 任务/会话事件：用于恢复 UI 输出、Codex 执行过程、Images job、V2Ray 运行事件，写入 events 并通过 SSE 展示，不用普通服务日志承载长输出。

服务日志应只记录关键边界和异常：

- 可以记录进程启动/关闭、监听地址、配置路径、数据库路径、迁移失败、存储失败、HTTP 5xx、panic、慢请求、安全限流、外部进程启动/退出失败、provider/network 失败、日志轮转/清理失败、tail/search/SSE 关键失败。
- 不要记录每个成功 HTTP 请求、每个成功事件 append、SSE heartbeat、普通轮询、正常 UI 查询、完整 stdout/stderr、完整 prompt、完整日志文件内容或高频成功路径。
- 对重复错误必须做聚合、采样或短窗口限频；不要在循环、tail、stream、event bridge 中逐条写无上限日志。

所有日志、audit、events 和前端日志响应都必须默认脱敏：

- 禁止记录 API Key、Authorization、cookie、session token、CSRF token、password、secret、私钥正文、完整 UUID、presigned URL query、图片 base64、data URL、完整远程图片 URL 中的敏感 query。
- 可以记录稳定 ID、模块名、操作名、状态、duration、错误摘要、文件路径摘要、workspace id、job id、session id、source id。
- 错误信息来自外部 provider、子进程 stderr 或用户输入时，写入前必须裁剪长度并做 secret redaction。

**个人自用部署的请求遥测豁免（与严格 SaaS 安全的差异）：**

本项目的定位是 owner 独占的个人服务，而不是面向多租户的 SaaS。为了在线排障优先级高于"最小数据采集"原则，
请求遥测的 path、remote IP、User-Agent 允许进入服务日志的异常/慢请求分支，但仍需遵守以下约束：

1. `path` 必须走 `safelog.RequestPathLabel`：只保留 URL path（`EscapedPath()`），无条件丢弃 query 与 fragment，避免 `?token=`、`?session_id=` 等敏感参数进入日志。`RequestPathLabel` 内部再经 `safelog.Text` 做长度裁剪和全局 redaction（Bearer、api_key=、AWS 签名等）二次防御。
   - 历史上曾用 `safelog.URLLabel` 处理相对 URL，但相对 URL 会回退到 `Text(raw)` 不剥离 query，因此**不要**在请求遥测场景复用 URLLabel。
2. `ip` / `User-Agent` 可以直出（均为异常排障的核心字段），但长度做上限裁剪，**永远不允许**作为 Git 仓库中的测试 fixture、示例配置或文档样例提交真实值。
3. 成功路径仍保持静默（不记录每个成功 HTTP 请求），仅 5xx、采样后的异常 4xx、非豁免慢请求会落到 service log — 保证排障可用性和日志量可控并存。
4. 如果未来接入公网代理或面向非 owner 用户暴露，该豁免必须移除并回退到"仅记录稳定 ID"的严格模式。

新增日志功能时还必须定义生命周期：

- 如果项目自己写日志文件，必须同时设计自动轮转和自动清理策略，包括最大文件大小、保留份数、保留天数和是否压缩。
- 如果日志由 systemd、journald、应用自身或外部 logrotate 管理，必须在设计或配置中明确 Phantom Lancer 只读查看，不负责删除。
- 日志读取 API 必须做路径白名单、规范化、最大字节数、最大行数、超时和权限校验；不允许一次性读取整个大文件。
- 长 stdout/stderr 应进入受控事件流或日志源，设置 ring buffer、事件数或字节数上限；服务 `slog` 只记录失败摘要。
